{                                        // Inicio del objeto de configuración raíz
  "server": { "port": 3000 },            // Puerto HTTP donde corre el backend Express (index.js)
  "db": {                                // Bloque de conexión a MySQL
    "host": "localhost",                 // Host de MySQL (localhost si es en la misma PC)
    "user": "museo",                     // Usuario de la base de datos
    "password": "museo2025",             // Password del usuario
    "database": "museo"                  // Nombre de la base de datos a usar
  },
  "embed": {                             // Bloque del microservicio de embeddings (Flask)
    "url": "http://127.0.0.1:5001/embed",// URL del endpoint POST /embed del servicio Python
    "timeout_ms": 3500                   // Tiempo máximo (ms) para esperar la respuesta del /embed
  },
  "search": {                            // Parámetros del buscador vectorial del backend
    "top_k": 4,                          // Cantidad máxima de candidatos a considerar (Top-K)
    "sim_threshold": 0.22,               // Umbral mínimo de similitud coseno para aceptar un doc
    "min_best_score": 0.74,              // Umbral que debe superar el MEJOR candidato para responder directo
    "snippet_chars": 280                 // Largo del extracto (snippet) mostrado en la respuesta
  },
  "disambiguation": {                    // Parámetros para menú de desambiguación
    "choice_delta": 0.02                 // Cuán “cerca” (en score) deben estar para ofrecer varias opciones
  }
}                                        // Fin del objeto
